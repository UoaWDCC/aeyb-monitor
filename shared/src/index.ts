// We need to export all the types we're using from this file for it to be usable in the
// other mono-repos
export * from './models/Test';

export default function hello() {
    return 'Hello world!';
}

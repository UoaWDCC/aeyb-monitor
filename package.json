{
    "name": "aeyb",
    "version": "0.1.0",
    "description": "aeyb fullstack",
    "scripts": {
        "removeModules": "rm -rf node_modules && cd frontend && rm -rf node_modules && cd ../backend && rm -rf node_modules",
        "installAll": "npm install && cd frontend && npm install && cd ../backend && npm install"
    },
    "dependencies": {
    },
    "devDependencies": {
        "typescript": "^4.6.4",
        "prettier": "^2.6.2",
        "@types/node": "^17.0.30",
        "@typescript-eslint/eslint-plugin": "^5.23.0",
        "@typescript-eslint/parser": "^5.23.0",
        "eslint": "^8.15.0"
    }
}
